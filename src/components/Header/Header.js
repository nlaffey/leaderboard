import React, {
    Component,
    PropTypes,
} from 'react';

class Header extends Component {
    render() {
        return (
            <div>[Header area]</div>
        );
    }
}

Header.propTypes = {};
Header.defaultProps = {};

export default Header;
