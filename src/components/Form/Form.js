var React = require('react');

var Form = React.createClass({
    render: function () {
        return (
            <div className="Form">
            </div>
        )
    }
});

module.exports = Form;